apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aws-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Rajeshkanimata/kubernetes-ingress-deployment.git'
    targetRevision: HEAD
    path: ARGOCD/AWS
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
